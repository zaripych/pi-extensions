{
  "name": "foundation",
  "private": true,
  "type": "module",
  "exports": {
    "./testing/harness/*": "./testing/harness/*.ts"
  },
  "scripts": {
    "format": "prettier --write -u .",
    "lint": "oxlint --type-aware",
    "lint-changed": "lint-staged --diff HEAD",
    "lint-staged": "lint-staged --hide-all",
    "prepare": "[ -d .git ] && husky || true",
    "test": "vitest run",
    "test-changed": "vitest run --changed",
    "typecheck": "tsgo --noEmit --pretty",
    "verify": "pnpm run \"/^(typecheck|lint|test-changed)$/\""
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260701.1",
    "expect-type": "1.3.0",
    "husky": "9.1.7",
    "lint-staged": "17.0.8",
    "oxlint": "1.71.0",
    "oxlint-tsgolint": "^0.24.0",
    "prettier": "3.8.4",
    "sort-package-json": "4.0.0",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
